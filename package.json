{
  "name": "@godalming123/signals",
  "description": "Push-based type-safe signals library for browsers, NodeJS, and bun.",
  "version": "0.2.7",
  "license": "BSD-3-Clause",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "check": "bun x tsc --noEmit",
    "test": "bun test ./signals.spec.ts --coverage",
    "debug": "bun --inspect-brk test signals.spec.ts --coverage --timeout=0",
    "build": "rm -rf build; bun x tsc"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "files": [
    "./license.md",
    "./package.json",
    "./readme.md",
    "./build/index.js",
    "./build/index.d.ts",
    "./build/core.js",
    "./build/core.d.ts",
    "./build/array.js",
    "./build/array.d.ts"
  ]
}
